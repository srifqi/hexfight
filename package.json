{
  "name": "hexfight",
  "version": "0.1.0",
  "description": "HexFight",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Just assume it's okay\" && exit 1",
    "preinstall": "rm -rf node_modules && npm cache clean",
    "heroku-prebuild": "rm -rf node_modules && npm cache clean"
  },
  "keywords": [
    "hexfight",
    "heroku"
  ],
  "author": "The hexfight team.",
  "license": "MIT",
  "engines": {
	"node": "7.2.1",
	"npm": "3.10.10"
  },
  "dependencies": {
    "express": "^4.14.0",
    "socket.io": "^1.7.2"
  }
}
